# Fitness app
### A full-stack fitness tracking application centered around Apache Cassandra as the primary database, 
### exploring its distributed, wide-column data model in a real-world context. The back-end is built with ASP.NET Core Web API (.NET 9),
### featuring JWT authentication with refresh tokens, while the front-end uses React with TypeScript. 
### The entire stack runs in a Dockerized environment.
